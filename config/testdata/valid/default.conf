// This is the default Grit configuration file.
//
// It illustrates the configuration that will be used if no configuration file
// (or an empty configuration file) is provided.

dir = "~/grit";

github {
    api = "https://api.github.com";
}
